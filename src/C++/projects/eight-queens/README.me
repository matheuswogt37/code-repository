# Eight Queens

Resolve the Problem "eight queens" using backtracking.

## Requirements

- A C++ compiler (e.g., `g++`)
- `make`

## Build and Run

To compile the program, run:

```bash
make
```

or

```
g++ main.cpp -o main.out
./main.out
```